use "user"

type Container {
	name: str

	print_num: fn (self: &Container, num: i32) {
		print(num)
	}

	print: fn (self: &Container) {
		print(&self.name)
	}
}

fn take_ptr(thing: &Container) {
	thing.print()
}

fn main {
	u.print()
	u.print_num(5)
	take_ptr(&u)

	i: i32 = 12
}
